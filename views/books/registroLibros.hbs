<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Libros</title>
    <link rel="stylesheet" href="../styles/styles.css">
    <style>
        /* Estilos para el mensaje flash */
        .flash-message {
            display: none;
            padding: 10px;
            background-color: yellow;
            border: 1px solid orange;
            text-align: center;
        }
    </style>
</head>

<body>
    <h1>Registro de Libros</h1>
    <div class="flash-message" id="flashMessage">¡Libro registrado con éxito!</div>
    <form action="/registro" method="post" id="bookForm">
        <!-- Agrega los campos del formulario según tus necesidades -->
        <label for="titulo">Título:</label>
        <input type="text" id="titulo" name="titulo" required>

        <label for="autor">Autor:</label>
        <input type="text" id="autor" name="autor" required>

        <label for="categoria">Categoría:</label>
        <input type="text" id="categoria" name="categoria" required>

        <label for="isbn">ISBN:</label>
        <input type="text" id="isbn" name="ISBN" required>

        <label for="copias">Copias Disponibles:</label>
        <input type="text" id="copias" name="copiasDisponibles" required>

        <button type="submit">Registrar Libro</button>
    </form>
    <div>
        <button onclick="window.location.href='http://127.0.0.1:3000/'">REGRESAR</button>
    </div>

    <script>
        // Mostrar el mensaje flash durante 3 segundos al enviar el formulario
        document.addEventListener("DOMContentLoaded", function() {
            var form = document.getElementById("bookForm");
            form.addEventListener("submit", function(event) {
                event.preventDefault(); // Evitar que el formulario se envíe (solo para el ejemplo)
                var flashMessage = document.getElementById("flashMessage");
                flashMessage.style.display = "block";
                setTimeout(function() {
                    flashMessage.style.display = "none";
                }, 3000); // Ocultar el mensaje después de 3 segundos (3000 milisegundos)
            });
        });
    </script>
</body>

</html>
